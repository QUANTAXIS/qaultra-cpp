@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(Threads)
find_dependency(nlohmann_json 3.2.0)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/qaultra-targets.cmake")

# Set variables for compatibility
set(QAULTRA_LIBRARIES qaultra)
set(QAULTRA_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include")

# Verify installation
check_required_components(qaultra)